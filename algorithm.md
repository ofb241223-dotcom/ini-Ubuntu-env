---
description: 解题工作流：分流 → 确认意图 → 陷阱扫描 → 执行分析 → 结构化输出
---

# /solve 解题工作流

## 第零阶段：问题分流 (Triage)

0. **错误类型识别**：若用户已明确错误类型，按以下路径裁剪后续阶段的检查重点：
   - **WA (Wrong Answer)**：重点检查逻辑正确性、边界条件、输出格式（行末空格、数值精度）。
   - **TLE (Time Limit Exceeded)**：重点检查算法复杂度、常数优化、死循环、容器效率（如禁止滥用 `std::endl`）。
   - **MLE (Memory Limit Exceeded)**：重点检查容器空间开销、递归深度、静态数组大小。
   - **RE/SegFault (Runtime Error)**：重点检查数组越界、递归爆栈、除零、空指针解引用。
   - **CE (Compile Error)**：直接分析编译器报错信息，跳过逻辑分析。
   - **未说明**：在后续步骤中主动询问，但初始扫描需覆盖以上所有关键点。

## 第一阶段：获取题目信息 (PTA Skill)

1. 读取文件头部的 `@pintia` 注释提取 `problemSetProblemId`，并使用 `find_by_name` 在缓存目录查找对应 JSON。
2. 提取并输出结构化摘要：标题、题意描述、约束条件（N 范围、时空限制）、样例输入输出。
3. 若缓存缺失，请求用户提供题目全文。

## 第 1.5 阶段：确认用户意图

4. **判断诉求类型**：
   - **"帮我 debug"**：采取“微创手术”模式。做最小化修改，逐个标注 bug，不进行大规模重构。
   - **"这题怎么写"**：提供完整的解题思路、算法选择理由以及符合规范的全新参考代码。
   - **"为什么不对/解释一下"**：定位根因，先通过反例解释逻辑缺陷，再给出具体修复建议。
5. **错误类型确认**：如果用户未提供错误点（WA/TLE/RE），必须在 Phase 3 开始前主动询问。

## 第二阶段：分析与陷阱扫描 (Algorithm Skill)

6. **执行高频 C++ 陷阱清单检查**（按优先级排序）：
   - **P0 [致命]**：
     - Range-for 中误用 `auto x` 而非 `auto& x` 导致修改无效。
     - 嵌套循环中内层变量名误用（如 `s[i]` 误写为 `s[j]`）。
     - 字符串执行 `erase/insert` 后，旧的长度变量未更新。
     - `if/else` 逻辑反转（本应每次执行语句被放进 else 分支）。
     - 多测中全局变量、容器、并查集等未归零/重置。
   - **P1 [逻辑]**：
     - Off-by-one 错误：倒数第 N 个与偏移量 N-1 的混淆。
     - 容器为空时未判断直接操作（如 `top/back`）导致崩溃或逻辑死循环。
     - `substr(pos, len)` 与 `substr(start, end)` 参数含义搞混。
     - 中间计算溢出：`a * b` 在转为 `long long` 前已溢出。
   - **P2 [格式与效率]**：
     - 行末多余空格（应先判断是否为首个/末个再输出空格）。
     - 浮点数比较未使用 `eps`，以及 C++ `%` 对负数的取模行为是否符合预期。

7. **专项扫描**：
   - **图论/树**：是否处理了不连通图、自环、重边；DFS 在 N ≥ 1e5 时必须检查栈深度或改迭代。
   - **I/O 冲突**：`getline` 与 `cin >>` 混用产生的换行符残留导致的空读。

## 第三阶段：输出结果

8. **输出诊断摘要**（置于回复头部）：
   > **诊断**：[WA / TLE / RE / 思路重排] | 发现 N 个问题 | 核心原因：[一句话说明]

9. **根据模式选择回复格式**：
   - **[修复模式]**：给出修改对照表
     | 行号/位置 | 原代码 | 修改后 | 原因 |
     | :--- | :--- | :--- | :--- |
     | ... | ... | ... | ... |
     每个 bug 用一句话说明核心逻辑，不长篇大论。
   - **[解释模式]**：用具体例子演示（输入 -> 原代码执行路径 -> 指出出错步骤 -> 正确逻辑）。
   - **[完整代码]**：仅当用户要求、改动 >5 处或属于“这题怎么写”类型时提供。

10. **干跑模拟规则**：
    - 涉及逻辑/算法正确性时必须干跑。若仅是语法/显式类型错误则跳过，直给结论。
    - 使用表格展示关键步骤的变量状态状态变化。
    - 针对发现的 bug 构造“最小反例”而非仅模拟原题样例。

## 强制规则

- **严禁 Lambda**：任何场景下（包括自定义 `sort`、`priority_queue` 比较器）均不得使用 lambda 表达式。请使用结构体 `operator()` 或全局/静态函数。
- **保持风格一致**：沿用用户原有 I/O（`scanf` 或加速后的 `cin`）和头文件风格，不主动替换 `<bits/stdc++.h>`。
- **禁止直接修改文件**：所有修改必须在对话框中以代码块形式提供，由用户自行处理。
- **命名规范**：除宏定义外，所有自定义变量和函数名必须纯小写。
- **简体中文**：所有解释、注释、摘要必须使用简体中文。
